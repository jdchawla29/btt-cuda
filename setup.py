# setup.py - minimal setup script that relies on pyproject.toml for configuration
# This is mainly for backward compatibility with older pip versions

from setuptools import setup

if __name__ == "__main__":
    setup()